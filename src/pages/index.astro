---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const posts = (await getCollection('blog', ({ data }) => data.draft !== true))
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 5);

const featured = posts[0];
const secondaryPosts = posts.slice(1);
---

<!doctype html>
<html lang="fr">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main class="home">
			<section class="hero" aria-labelledby="hero-title">
				<div>
					<p class="eyebrow">IA générale, gros modèles, sans filtre</p>
					<h1 id="hero-title">Comprendre les avancées de l'IA avant que le buzz ne prenne le contrôle.</h1>
					<p class="lede">{SITE_DESCRIPTION}</p>
					<div class="hero-actions">
						<a class="button" href="/blog/">Lire les articles</a>
						<a class="button-secondary" href="/about/">Notre ligne éditoriale</a>
					</div>
				</div>
				<aside class="terminal-card" aria-label="Résumé des sujets couverts">
					<div class="terminal-top"><span></span><span></span><span></span></div>
					<pre><code>$ veille --focus
foundation-models
scaling-laws
multimodal-agents
alignment-safety
industry-shifts
sources: required</code></pre>
				</aside>
			</section>

			<section class="pillars" aria-labelledby="pillars-title">
				<p class="eyebrow">Le terrain</p>
				<h2 id="pillars-title">Des analyses techniques, pas du marketing.</h2>
				<div class="pillar-grid">
					<article class="card pillar">
						<h3>Modèles & architectures</h3>
						<p>Décrypter les nouvelles architectures, les scaling laws, les modèles multimodaux et ce qui change vraiment.</p>
					</article>
					<article class="card pillar">
						<h3>Industrie & enjeux</h3>
						<p>Open-weight vs closed, course aux GPU, régulation, et les vrais enjeux économiques derrière les annonces.</p>
					</article>
					<article class="card pillar">
						<h3>Guides sourcés</h3>
						<p>Chaque article vise une décision concrète, avec sources en bas de page et limites explicites.</p>
					</article>
				</div>
			</section>

			{featured && (
				<section class="latest" aria-labelledby="latest-title">
					<div class="section-heading">
						<p class="eyebrow">À lire maintenant</p>
						<h2 id="latest-title">Derniers articles</h2>
					</div>
					<div class="latest-layout">
						<article class="featured-card">
							<a href={`/blog/${featured.id}/`}>
								<span><FormattedDate date={featured.data.pubDate} /></span>
								<h3>{featured.data.title}</h3>
								<p>{featured.data.description}</p>
							</a>
						</article>
						<ul class="compact-list">
							{secondaryPosts.map((post) => (
								<li>
									<a href={`/blog/${post.id}/`}>
										<span><FormattedDate date={post.data.pubDate} /></span>
										<strong>{post.data.title}</strong>
									</a>
								</li>
							))}
						</ul>
					</div>
				</section>
			)}
		</main>
		<Footer />
	</body>
</html>

<style>
	.home {
		width: min(var(--container), calc(100% - 2rem));
	}

	.hero {
		display: grid;
		grid-template-columns: minmax(0, 1.2fr) minmax(320px, 0.8fr);
		align-items: center;
		gap: clamp(2rem, 6vw, 4rem);
		padding: clamp(2.5rem, 7vw, 6rem) 0 3rem;
	}

	.hero h1 {
		max-width: 900px;
	}

	.hero-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
		margin-top: 1.6rem;
	}

	.terminal-card {
		border: 1px solid #263244;
		border-radius: var(--radius-lg);
		background: #111827;
		box-shadow: var(--shadow-soft);
		overflow: hidden;
	}

	.terminal-top {
		display: flex;
		gap: 0.45rem;
		border-bottom: 1px solid #263244;
		padding: 0.8rem 1rem;
	}

	.terminal-top span {
		width: 0.72rem;
		height: 0.72rem;
		border-radius: 999px;
		background: #8ee6d4;
		opacity: 0.78;
	}

	.terminal-top span:nth-child(2) {
		background: #f2c078;
	}

	.terminal-top span:nth-child(3) {
		background: #df8d8d;
	}

	.terminal-card pre {
		margin: 0;
		border: 0;
		border-radius: 0;
		box-shadow: none;
	}

	.pillars,
	.latest {
		margin-top: clamp(2.5rem, 7vw, 5rem);
	}

	.pillars > h2 {
		max-width: 720px;
	}

	.pillar-grid {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1rem;
		margin-top: 1.5rem;
	}

	.pillar {
		padding: 1.25rem;
	}

	.pillar h3 {
		font-size: 1.25rem;
	}

	.pillar p {
		margin-bottom: 0;
		color: var(--text-muted);
		font-size: 0.98rem;
	}

	.section-heading {
		margin-bottom: 1.5rem;
	}

	.latest-layout {
		display: grid;
		grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr);
		gap: 1rem;
	}

	.featured-card,
	.compact-list li {
		border: 1px solid var(--border);
		border-radius: var(--radius-lg);
		background: color-mix(in srgb, var(--surface) 88%, transparent);
	}

	.featured-card {
		padding: clamp(1.4rem, 4vw, 2rem);
		background:
			linear-gradient(135deg, rgba(216, 243, 238, 0.72), transparent 60%),
			var(--surface);
	}

	.featured-card a,
	.compact-list a {
		color: inherit;
		text-decoration: none;
	}

	.featured-card span,
	.compact-list span {
		display: inline-block;
		margin-bottom: 0.65rem;
		color: var(--accent-dark);
		font-family: var(--font-mono);
		font-size: 0.78rem;
		font-weight: 760;
	}

	.featured-card h3 {
		font-size: clamp(1.7rem, 4vw, 2.7rem);
	}

	.featured-card p {
		color: var(--text-muted);
	}

	.compact-list {
		display: grid;
		gap: 0.75rem;
		list-style: none;
		margin: 0;
		padding: 0;
	}

	.compact-list li {
		margin: 0;
		padding: 1rem;
	}

	.compact-list strong {
		display: block;
		font-size: 1rem;
		line-height: 1.35;
	}

	@media (max-width: 860px) {
		.hero,
		.latest-layout {
			grid-template-columns: 1fr;
		}

		.pillar-grid {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 720px) {
		.home {
			width: min(100% - 1rem, var(--container));
		}
	}
</style>
